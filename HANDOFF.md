# Handoff: attention-wins-elections

Run this session with `claude --dangerously-skip-permissions` from `/Users/kasey/Desktop/attention-wins-elections`.

## Project summary

Testing whether candidates who receive more media attention tend to win elections.

Two pre-registered hypotheses:
- **H1**: The candidate with more mentions wins >50% of the time (binomial test vs. 0.5)
- **H2**: Mention share positively correlates with vote share (Pearson r)

All figures are generated by `scripts/generate_figures.py`. All data lives in `data/raw/`. Only real data is used — no synthetic data. Run the script with `.venv/bin/python3 scripts/generate_figures.py` from the project root.

## Current figure inventory

| Figure | File | Status |
|--------|------|--------|
| Fig 1 | fig1_h1_win_rate.png | ✓ Real data |
| Fig 2 | fig2_h2_scatter.png | ✓ Real data |
| Fig 3 | fig3_gap_and_timeline.png | ✓ Real data |
| Fig 4 | fig4_win_rate_comparison.png | ✓ Real data (4 levels incl. Governor) |
| Fig 5 | fig5_house_seat_share.png | ✓ Real data |
| Fig 6 | fig6_gap_distribution.png | ✓ Real data (4 levels incl. Governor) |
| Fig 7 | fig7_multi_source_forest.png | ✓ Real data (4 levels; 2 sources pending) |
| Fig I1 | figI1_international_h1.png | ✓ Real data (UK + Australia PM-level) |
| Fig I2 | figI2_international_h2.png | ✓ Real data (UK + Australia PM-level) |
| Fig I3 | figI3_uk_commons.png | ✓ Real data (UK Commons party-vs-seats) |
| Fig S1 | figS1_by_era.png | ✓ Real data |
| Fig S2 | figS2_window_sensitivity.png | ✓ Real data (Ngrams 1/2/3-year windows) |

## Data collected so far

### Google Ngrams (main source — books corpus through 2019)
- **Presidential** (1960–2016, n=15): H1=80%, p=0.035. H2: r=0.42. `data/raw/ngrams/presidential_mention_shares.csv`
- **Senate** (1976–2018, n=521 usable): H1=77%, p<0.0001. H2: r=0.625. `data/raw/ngrams/senate_mention_shares.csv`
- **House** (party-level, 1962–2018, n=29 cycles): H1=66%, p=0.136. H2: r=0.595. `data/raw/ngrams/house_attention_shares.csv`
- **UK** (1945–2019, n=18 usable): H1=72%, p=0.096. H2: not yet in figures. `data/raw/ngrams/uk_mention_shares.csv`
- **Australia** (1949–2019, n=24 usable): H1=75%, p=0.023. `data/raw/ngrams/australia_mention_shares.csv`
- **Governors** (1960–2018, n=753 usable): H1=69%, p<0.0001. H2: r=0.240, p<0.0001. `data/raw/ngrams/governors_mention_shares.csv`. Returns collected from Wikipedia race-page infoboxes (`scripts/collect_governors_wikipedia.py`) — MEDSL has no candidate-level governor dataset. Now a 4th level in Figs 4, 6, 7.

### Other sources (presidential only)
- Google Trends: `data/raw/trends/presidential_mention_shares.csv`
- Wikipedia Pageviews: `data/raw/wikipedia/presidential_mention_shares.csv`
- GDELT News: `data/raw/gdelt_news/presidential_mention_shares.csv`
- Reddit: `data/raw/reddit/presidential_mention_shares.csv`
- Senate Trends (2016–2020, n=92): `data/raw/trends/senate_mention_shares.csv`
- Senate Wikipedia (2016–2020, n=71): `data/raw/wikipedia/senate_mention_shares.csv`

## Governor data collection (DONE — was the blocking task)

The old `collect_ngrams_governors.py` used Harvard Dataverse file IDs that returned Senate data. MEDSL has **no** candidate-level governor dataset (verified by searching the `medsl` Dataverse — it has President/Senate/House constituency returns only). Klarner's "Governors Dataset" (doi:10.7910/DVN/PQ0Y1N) has governor *names* but no opponent/vote-share pairs.

**Solution used:** `scripts/collect_governors_wikipedia.py` scrapes Wikipedia race-page infoboxes (`{year} {state} gubernatorial election`). For each year it lists the category members, fetches infobox wikitext, parses nominees/parties/percentages (handles `{{nowrap}}`, `{{percent|v|t}}` templates, state party names, nickname/suffix stripping), and keeps races where the top two are one D and one R. Output: `data/raw/governors_cleaned.csv` (801 D-vs-R races, 1960–2018, 50 states; territories dropped).

`collect_ngrams_governors.py` now reads that CSV, maps state names → postal codes, and runs the same `ngrams_pair()` loop. `ngram_term()` strips suffixes (Jr./III) and converts `'W. P. "Bill" Atkinson'` → `'Bill Atkinson'` for queryable terms. Result: **753 usable, H1=69% (p<0.0001), H2 r=0.240 (p<0.0001)**.

Governors are now integrated as a 4th level in Figs 4, 6, and 7. (Figs I1/I2 are international-only — no governor panel.) While there, fixed a latent Fig 4 Panel A bug where jittered scatter numeric x-positions polluted the category axis; Panel A is now a numeric axis with explicit ticks.

## Key code notes

- `generate_figures.py` imports all CSVs at the top. Senate vote_pct is stored as a **fraction** (0.54), presidential as a **percentage** (49.7). Code converts pres to fraction with `/ 100`.
- Color convention: D=`#1060C8` (blue), R=`#C82010` (red). Attention winner=`#2c7873` (teal), upset=`#f4a261` (amber). Do NOT use red/blue for attention outcomes.
- OLS r values are annotated inline near the regression line using `ols_label()` helper — not in the legend.
- Fig 7 is a single-panel H1 forest plot (panel A only). B/C/D were intentionally removed.
- The `.venv` Python environment has all dependencies. Always use `.venv/bin/python3`.

## Recently completed (this session)

- **1960 collection bug fixed.** `collect_ngrams.py` floored the window start at 1960, making `year_start == year_end` for the 1960 race; the Ngrams API then returns the whole-corpus series and the year-mapping zeroed both candidates (forced 0.5/0.5, miscounted as an attention-leader loss). Now uses a 2-year window `[year-1, year]` with a length guard. 1960 is now a *real* attention upset (Kennedy trailed Nixon in book mentions, won the popular vote). Headline H1 unchanged at 12/15 (80%, p=0.035).
- **Fig S2 built** — window-length sensitivity (`collect_ngrams_windows.py` → `presidential_windows.csv`). H1 identical (80%) across 1/2/3-year windows; H2 r = 0.36–0.40.
- **Fig 6** — switched to violin + box + raw points, plotted on `log10(gap)` linear axis so the KDE is sensible; "Equal attention (1×)" label moved off the presidential box.
- **Fig 7** — dropped the empty pending rows, Ngrams moved to top with all 4 levels spread + `n=` labels, small-n rows (n<5) faded so their wide CIs don't dominate.
- **UK section** — added `collect_uk_commons.py` → `uk_commons.csv` and **Fig I3** (UK Commons party attention vs. seat share, now 13/19, r=0.38). No UK Senate/Governor analogue exists with quality Ngrams data.
- **Fig I2 fixed to use both candidates** (was winner-only, a selection/collider artifact that flipped r negative). Positive in every country now.
- **Fig 4 is now 3 panels**: (A) win rate, (B) attention leader's vote/seat share (continuous, clusters >50%), (C) vote margin.
- **Pre-1960 international bug fixed** (`ngrams_pair` floored year_start at 1960 → invalid range → wrong decade-long series for every pre-1960 election). Fixed; re-collection added usable years (UK 17→19, Aus 24→28), no winners flipped.
- **Added Canada + New Zealand.** International now 4 countries (UK/Aus/Canada/NZ), pooled **65/90 = 72%, p<0.001**. Figs I1/I2 are 2×2. Canada/NZ vote shares need user verification before formal publication (winners verified via Wikipedia).
- **README fully rewritten**; removed stale `*_FAKE.csv` + `generate_fake_data_and_figures.py`.

## In progress / remaining (user requested A+B+C+D expansions)
- [x] A: Canada, New Zealand (done)
- [ ] B: GDELT TV (broadcast) source for Fig 7
- [ ] C: verify/extend Trends + Wikipedia senate coverage
- [ ] D: US presidential primaries (purest attention test)
- **State assembly elections**: too many obscure candidates for Ngrams. Skip.

## Git state

Branch: `main`. Repo has no remote. Commit after each meaningful step.
